# Postgres
POSTGRES_DB=trading_platform
POSTGRES_USER=trader_user
POSTGRES_PASSWORD=trader_password
POSTGRES_PORT=5432

# API (used by Node / Prisma, not by Postgres container itself)
API_PORT=4000
DATABASE_URL=postgresql://trader_user:trader_password@localhost:5432/trading_platform
